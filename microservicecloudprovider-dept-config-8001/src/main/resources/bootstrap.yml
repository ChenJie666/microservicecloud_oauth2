spring:
  cloud:
    config:
      name: microservicecloud-config-dept-client #需要读取的GitHub上的文件名，注意省略.xml
      profile: dev #本次访问的配置项
      label: master
      uri: http://config-3344.com:3344  #本微服务启动后先去找3344，通过SpringCloudConfig获取GitHub的服务地址