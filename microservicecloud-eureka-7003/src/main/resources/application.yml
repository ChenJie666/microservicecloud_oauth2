#集群配置
server:
  port: 7003

eureka:
  instance:
    hostname: eureka3.com #hosts中添加映射
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://eureka1.com:7001/eureka/,http://eureka2.com:7002/eureka/
